export type Image = {
    src: string;
    alt: string;
}

export type Meta = {
    heading: Heading;
    slug: string;
}

export type Heading = "Amazes" | "Informs" | "Amuses" | "Inspires" | "Feed"
export type Size = "small" | "medium" | "large" | "huge"

